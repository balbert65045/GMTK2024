using UnityEngine;

public class Debugger : MonoBehaviour
{
    private void Update()
    {
        /*
        if (Input.GetKeyDown(KeyCode.Alpha8))
        {
            WebResourceController.Instance.IncrementWebCount(1);
        }
        if (Input.GetKeyDown(KeyCode.Alpha9))
        {
            WebResourceController.Instance.DecrementWebCount(1);
        }
        if (Input.GetKeyDown(KeyCode.Q))
        {
            GameManager.Instance.ToggleGameMode();
        }

        if (GameManager.Instance.Mode == GameMode.BUILD_MODE)
        {
            if (Input.GetKeyDown(KeyCode.Alpha1))
            {
                InventoryController.Instance.SelectBlockViaHotKey(1);
            }
            if (Input.GetKeyDown(KeyCode.Alpha2))
            {
                InventoryController.Instance.SelectBlockViaHotKey(2);
            }
            if (Input.GetKeyDown(KeyCode.Alpha3))
            {
                InventoryController.Instance.SelectBlockViaHotKey(3);
            }
            if (Input.GetKeyDown(KeyCode.Alpha4))
            {
                InventoryController.Instance.SelectBlockViaHotKey(4);
            }
            if (Input.GetKeyDown(KeyCode.Alpha5))
            {
                InventoryController.Instance.SelectBlockViaHotKey(5);
            }
            if (Input.GetKeyDown(KeyCode.Alpha6))
            {
                InventoryController.Instance.SelectBlockViaHotKey(6);
            }
        }

        */
    }
}
